FROM node:22-alpine AS runtime
RUN corepack enable && corepack prepare pnpm@10.14.0 --activate
WORKDIR /app
COPY . .
RUN pnpm install --frozen-lockfile && pnpm --filter @token-farmer/api build
ENV NODE_ENV=production
EXPOSE 4000
CMD ["pnpm", "--filter", "@token-farmer/api", "start"]
