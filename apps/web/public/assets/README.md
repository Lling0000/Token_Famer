# Token Farmer visual assets

The desktop client currently consumes:

- `token-farm-background.png`: full-bleed pixel farm atmosphere used behind authentication and the playable canvas.

The interactive plots, crops, water, issues, barn, trees, and pond are rendered by the canvas fallback, so the farm remains usable if the bitmap is unavailable.
